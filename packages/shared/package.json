{
  "name": "@kosensai/shared",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist",
    "lint": "echo 'lint not configured'",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.3",
    "hono": "^4.6.8",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@kosensai/config": "workspace:*",
    "@types/node": "^22.10.2",
    "tsup": "^8.3.5",
    "typescript": "^5.8.3"
  }
}

